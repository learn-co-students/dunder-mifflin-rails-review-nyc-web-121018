<h1>All the Dogs</h1>

<ul id = "dogs">
  <% @dogs.each do |dog| %>
  <li><%= link_to dog.name, dog_path(dog) %></li>
  <% end %>
</ul>

<ul id = "dogs">
  <% @sorted_dogs.each do |dog| %>
  <li><%= link_to dog.name, dog_path(dog) %></li>
  <% end %>
</ul>
